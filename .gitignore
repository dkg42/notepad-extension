# Default ignored files
/shelf/
/.idea
# Ignored default folder with query files
/queries/
# Datasource local storage ignored files
/dataSources/
/dataSources.local.xml
# Editor-based HTTP Client requests
/httpRequests/

# WXT
.output/
.output-dev/
.wxt/

# Env profile files — never commit. Build values live in .env.<mode>.local
# (copy from .env.example). .env.example is the only checked-in env file.
.env.local
.env.*.local
.env.development
.env.production

# Dependencies
node_modules/

# macOS
.DS_Store
